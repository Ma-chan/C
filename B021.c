#include <stdio.h>
int main(void){
    // 自分の得意な言語で
    // Let's チャレンジ！！

    int N;
    scanf("%d",&N);
    char a[N];
    for(i=0;i<N;i++){
        scanf("%s",&a[i]);
    }
    if(a[i][strlen(a[i])]){
        
    }else if(){
        
    }else if( && ){
        
    }
}
